import { HexBase64Latin1Encoding } from "crypto";
import React, { useState } from "react";
import { SendRequestWebService } from "./userRequest";
import { Select } from "antd";
/* let array = SendRequestWebService();

export function TstArray(props) {
  //const [arrayCause, setArrayCause] = useState([]);

  props.array.then((cause) => {
    //console.log(cause[1].desDem);
    for (var val in cause) {
      //console.log(cause[val].desDem);
      props.setArrayCause(cause[val].desDem);
    }
    console.log(arrayCause);
  });
  //console.log(arrayCause);

  return props.arrayCause;
} */
